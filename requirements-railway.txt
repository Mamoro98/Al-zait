# Al Zait Railway-Optimized Requirements (Under 4GB Docker Image)
# This version removes heavy AI dependencies for Railway free tier deployment

# Core dependencies
langchain>=0.2.0
langgraph>=0.2.0
langchain-community>=0.2.0

# LLM providers (lightweight options only)
groq>=0.4.0
google-generativeai>=0.8.0
# ollama>=0.1.0  # Removed - too heavy for Railway

# News and data sources
newsapi-python>=0.2.6
feedparser>=6.0.0  
requests>=2.28.0

# Telegram bot
python-telegram-bot>=20.0

# Database (sqlite3 is built-in to Python)

# Scheduling and automation
APScheduler>=3.10.0

# Basic audio support (lightweight)
gTTS>=2.3.0  # Google TTS - lightweight and free
# pydub>=0.25.0  # Removed - requires ffmpeg, adds size
# mutagen>=1.47.0  # Removed - not essential

# Basic text processing (instead of heavy ML)
textdistance>=4.5.0  # Lightweight text similarity
python-Levenshtein>=0.21.0  # Fast string matching

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
loguru>=0.7.0
psutil>=5.9.0  # System monitoring

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0

# REMOVED FOR RAILWAY OPTIMIZATION:
# chromadb>=0.4.0  # 🚫 Vector database - too heavy (~2GB)
# sentence-transformers>=2.2.0  # 🚫 ML embeddings - includes PyTorch (~3GB)  
# torch>=1.11.0  # 🚫 PyTorch - very heavy (~2GB)
# onnxruntime>=1.14.1  # 🚫 ML runtime - heavy
# scipy>=1.7.0  # 🚫 Scientific computing - large
# scikit-learn>=1.0.0  # 🚫 ML library - heavy

# LIGHTWEIGHT ALTERNATIVES USED:
# ✅ textdistance - Simple text similarity (vs ChromaDB)
# ✅ python-Levenshtein - Fast string matching (vs ML embeddings)
# ✅ Groq/Gemini APIs - Cloud LLM (vs local Ollama)
# ✅ Basic keyword matching - Simple search (vs vector search)
# ✅ gTTS only - Basic audio (vs advanced audio processing)
